{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "./sections/desktop.schema.json",
  "title": "Desktop Section",
  "description": "Schema for desktop customization settings used in group_vars overlay files.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "background_image_file": {
      "$ref": "#/$defs/asset_ref",
      "description": "Typed asset reference for the desktop background image copied from config/assets during overlay rendering."
    },
    "shortcuts_in_menu": {
      "type": "array",
      "description": "List of application shortcut IDs shown in the desktop menu.",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "minItems": 1
    }
  },
  "$defs": {
    "asset_ref": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "type": {
          "const": "asset",
          "description": "Reference type. Must be 'asset'."
        },
        "path": {
          "type": "string",
          "description": "Relative path under config/assets. For backward compatibility, values prefixed with assets/ are also accepted.",
          "minLength": 1
        }
      },
      "required": [
        "type",
        "path"
      ]
    }
  }
}